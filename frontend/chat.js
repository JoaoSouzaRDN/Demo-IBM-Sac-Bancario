const options=['Fiz um Pix e o dinheiro não chegou','Perdi meu cartão','Quero contestar uma compra','Minha parcela está atrasada','Quero atualizar meus dados'];const $=s=>document.querySelector(s);function add(t,w='agent'){ $('#messages').insertAdjacentHTML('beforeend',`<div class="msg ${w}">${t}</div>`)}function start(){ $('#messages').innerHTML='';$('#suggestions').style.display='flex';add('Olá! Sou o assistente do RDN Bank. Vou consultar os dados e orientar você com segurança. Como posso ajudar?');$('#suggestions').innerHTML=options.map(x=>`<button>${x}</button>`).join('');document.querySelectorAll('.suggestions button').forEach((b,i)=>b.onclick=()=>{ $('#suggestions').style.display='none';send(options[i])})}async function send(t){if(!t)return;add(t,'user');$('#input').value='';add('Consultando os sistemas do banco…');try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:t})});const d=await r.json();document.querySelectorAll('.agent').at(-1).remove();add(d.reply||d.output||'Atendimento concluído.')}catch(e){document.querySelectorAll('.agent').at(-1).remove();add('Não foi possível conectar ao agente.')}}$('#form').onsubmit=e=>{e.preventDefault();send($('#input').value.trim())};$('#reset').onclick=start;start();
